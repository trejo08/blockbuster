require 'spec_helper'
require '../app/models/customer'

describe Customer do
  # let(:trigger) { build(:trigger) }

  # it 'creates a valid trigger factory' do
  #   expect(trigger).to be_valid
  #   expect(trigger.save!).to be true
  # end

  # it "validates presences of 'text' field" do
  #   trigger.text = nil
  #   expect(trigger.save).to be false
  # end

  # it 'sets conditions' do
  #   trigger = create(:trigger, cond_column: 'patient', cond_equality: '==', cond_value: 'value')
  #   expect(trigger.conditions).to eq("patient == 'value'")
  # end

end
